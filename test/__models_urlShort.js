const expect = require('chai').expect;
const Url = require('../models/data/urlShort');

describe('url model', () => {
  it('Create url', () => {});
  it('Read all urls', () => {});
  it('Read url by id', () => {});
  it('Redirect', () => {});
  it('Update url', () => {});
  it('Delete url', () => {});
});
