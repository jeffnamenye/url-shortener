const expect = require('chai').expect;
const User = require('../models/data/user');

describe('user model', () => {
  it('Create user', () => {});
  it('Read all users', () => {});
  it('Read user by id', () => {});
  it('Update user', () => {});
  it('Delete user', () => {});
});
